<h1><%= @student.first_name %></h1>
<h1><%= @student.last_name %></h1>
<% if @student.active %>
  This student is currently active.
<% else %>
  This student is currently inactive.
<% end %>
<%= link_to "Activate/deactivate", "#{students_path(@student)}/activate" %>
